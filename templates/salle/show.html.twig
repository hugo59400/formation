{% extends 'base.html.twig' %}

{% block title %}Salle{% endblock %}

{% block body %}
    <h1>Salle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salle.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ salle.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_salle_index') }}">retour liste</a>

    <a href="{{ path('app_salle_edit', {'id': salle.id}) }}">Modif</a>

    {{ include('salle/_delete_form.html.twig') }}
{% endblock %}
