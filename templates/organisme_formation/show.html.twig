{% extends 'base.html.twig' %}

{% block title %}OrganismeFormation{% endblock %}

{% block body %}
    <h1>OrganismeFormation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ organisme_formation.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ organisme_formation.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ organisme_formation.adresse }}</td>
            </tr>
            <tr>
                <th>Num_tel</th>
                <td>{{ organisme_formation.numTel }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ organisme_formation.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_organisme_formation_index') }}">retour liste</a>

    <a href="{{ path('app_organisme_formation_edit', {'id': organisme_formation.id}) }}">Modif</a>

    {{ include('organisme_formation/_delete_form.html.twig') }}
{% endblock %}
